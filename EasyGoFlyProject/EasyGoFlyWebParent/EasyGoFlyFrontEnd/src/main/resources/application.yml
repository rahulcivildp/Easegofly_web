logging:
  file:
    name: logs/application.log
  level:
    root: INFO
server:
  port: 443
  servlet:
    context-path: /
    session:
      cookie:
        http-only: true
        same-site: none
        secure: true
      timeout: 60m
      tracking-modes: cookie
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    password: tanmay@311
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mysql://127.0.0.1:3306/easygofly
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format-sql: true
        globally-quoted-identifiers: true
        use-sql-comments: true
    show-sql: false
  main:
    allow-circular-references: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
  security:
    oauth2:
      client:
        registration:
          google:
            scope:
              - email
              - profile
            client-id: 640385530645-4ccu1nfvso6gclg03e9hbi6lueko2qta.apps.googleusercontent.com
            client-secret: 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
